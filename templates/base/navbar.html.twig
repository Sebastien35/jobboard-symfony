<link rel="stylesheet" href="{{ asset('stylesheets/nav.css') }}">

<header>
    <nav class="navbar  bg-grey">
        <div class="container-fluid d-flex ">
        <a class="navbar-brand text-dark" href=" {{ url('app_home')}}" >JOBBOARD</a>
        <button class="navbar-toggler text-dark" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <img src="{{ asset('brand-assets/logos/J-blue.svg')}}" alt="logo" width="30" height="30">
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link text-dark" href="{{ url('app_home')}}">Home</a>
                {% if app.user %}
                <a class="nav-link text-dark" href="{{ url('app_logout')}}">Logout</a>
                {% else %}
                <a class="nav-link text-dark" href="{{ url('app_login')}}">Login</a>
                <a class="nav-link text-dark" href="{{ url('app_register')}}">Register</a>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                <a class="nav-link text-dark" href="{{ url('app_admin_index')}}">Admin</a>
                {% endif %}

                
            </li>
        </div>
    </nav>
</header>